<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{titulo}} | Store Manager</title>
    <link href="/css/bootstrap.min.css" rel="stylesheet"> 
    <link href="/css/style.css" rel="stylesheet">
</head>
<body>
    
    {{! BARRA DE NAVEGACIÓN (MENÚ) }}
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container-fluid">
            <a class="navbar-brand" href="/">Store Manager</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                    
                    {{! --------------------- SECCIONES DE ADMINISTRACIÓN --------------------- }}
                    {{! Estas secciones generalmente son solo para el administrador }}
                    <li class="nav-item">
                        <a class="nav-link" href="/categorias">Categorías</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/productos">Productos</a>
                    </li>
                    
                    {{! --------------------- SECCIONES DE USUARIO FINAL --------------------- }}
                    <li class="nav-item">
                        <a class="nav-link" href="/catalogo">Catálogo</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/carrito">Carrito</a>
                    </li>

                </ul>
                
                {{! --------------------- INFORMACIÓN DEL USUARIO Y LOGOUT --------------------- }}
                {{#if usuario}}
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <span class="nav-link text-white-50">Hola, {{usuario.nombre}}</span>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link btn btn-outline-danger ms-2" href="/logout">Cerrar Sesión</a>
                    </li>
                </ul>
                {{/if}}
            </div>
        </div>
    </nav>

    {{! CONTENIDO PRINCIPAL }}
    <div class="container mt-4">
        {{! Aquí se inyectará el contenido de las vistas (ej: home.hbs, productos.hbs) }}
        {{{body}}} 
    </div>

    <script src="/js/bootstrap.bundle.min.js"></script>
</body>
</html>